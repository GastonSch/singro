services:
  captions:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      CAPTIONS_CONFIG: sessions.yaml
    volumes:
      - ./samples:/app/samples:ro
      - ./sessions.yaml:/app/sessions.yaml:ro
    restart: unless-stopped
